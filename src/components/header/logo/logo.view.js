import React from 'react';
import styles from './logo.module.css';

const Logo = () => (
  <div className={styles.container}>
    You
    <span className={styles.sufix}>Time</span>
  </div>
);

export default Logo;
